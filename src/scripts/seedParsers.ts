// This script was a one-time migration tool to seed file-based parsers into the DB.
// FileParserLoader has been removed — parsers are now created and managed via the UI.
console.error('seedParsers is deprecated. Create parsers via the web UI instead.')
process.exit(1)
