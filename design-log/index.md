# Design Log Index

Catalog of design logs. Append entries here when a new log is created or its scope materially changes.

Status meanings:

- **drafted** — design written; open questions, scope or shape may still move.
- **locked** — design frozen; implementation can begin, no further design churn expected.
- **completed** — implemented and verified; log carries an Implementation Results section.

| #   | Title                                                                 | Status    | Description                                                                                                                                                                                                                                                                                 |
| --- | --------------------------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001 | [Starter: DDD scraper platform + Express API + React client](001-starter.md) | completed | Universal web scraping platform. DDD layering (domain/application/infrastructure/cli/api), one Worker Thread per step, Playwright as primary browser adapter, PostgreSQL + Drizzle ORM for persistence, Express REST + SSE API, React + Vite client. |
| 002 | [Failed page HTML capture](002-failed-page-html-capture.md) | completed | Capture the browser HTML at the moment a page job permanently fails and surface it in the Task Detail UI. Workers send HTML in PAGE_FAILED, orchestrator emits task_failed_html after final retry, persisted to taskResults as a sentinel row, shown in collapsible viewer. |
| 003 | [Monaco-based JSON editor](003-json-editor.md) | completed | Replace all JSON configuration textareas with a Monaco Editor in JSON mode. Syntax highlighting, inline error squiggles, and a Format button wired to Monaco's built-in document formatter. No new dependency — Monaco was already in the bundle for the step code editor. |
| 004 | [Browser type visibility and DbParserLoader fix](004-browser-type-visibility.md) | completed | Fix DbParserLoader silently dropping the browserType DB column (stealth adapter never activated). Surface browserType on RunInfo via a parsers leftJoin and display it in the Task Detail info grid. Reword the misleading "Not captured" HTML fallback message. |
| 005 | [JsonEditor stale closure fix](005-json-editor-stale-closure.md) | completed | Fix Monaco blur handler always reading stale empty state. onDidBlurEditorText registered the onBlur prop once at mount; subsequent renders created new closures over current state that were never registered. Fixed with an onBlurRef kept current via useEffect. |
| 006 | [Bot detection evasion: delays and context rotation](006-bot-detection-evasion.md) | completed | Defeat session-based bot detection (observed on boat24.com). Random inter-request delays (pageDelayMin/pageDelayMax). Context rotation every N pages (maxPagesPerContext). Failure rotation: immediate and unconditional — fires on any page failure without waiting for concurrent pages, kills siblings, contextKilledCount prevents spurious second rotation. Both workers. |
| 007 | [Architecture hardening: security, concurrency correctness, persistence integrity](007-architecture-hardening.md) | completed | Senior architecture review findings across security (path traversal, SSRF, CORS), concurrency correctness (globalActive desync, data-on-stop loss, worker crash recovery, CSV overwrite), persistence integrity (fire-and-forget writes, missing transactions/indexes, __failedHtml sentinel), and layering (settings-merge duplication, stats divergence, CSV port injection). |
| 008 | [Enriched parser list: per-parser stats in a single query](008-enriched-parser-list.md) | completed | Single SQL query (DISTINCT ON CTE + conditional aggregates) added to RunPersistenceService to return status, last run date, and task success rate for all parsers in one round-trip, replacing the N+1 pattern the parsers page rebuild would otherwise require. |
| 009 | [Monaco autocomplete for Playwright page.* and task.* APIs](009-monaco-playwright-completions.md) | completed | Static dot-triggered completion provider registered on the Monaco JavaScript language; covers 20 Playwright Page methods and 2 task fields. Module-level guard prevents duplicate provider registration on step switch. |
| 010 | [JSON and Excel output formats via OutputWriter factory](010-output-writer-factory.md) | completed | OutputWriter interface with CsvWriter, JsonWriter (streaming), and ExcelWriter (in-memory) implementations unified behind a createOutputWriter factory. Format selected per step via outputFormat in StepSettings; CsvPostProcessor skipped for non-CSV formats. |
| 011 | [Full parser config roundtrip via JSON export/import](011-parser-import-export.md) | completed | GET /:id/export strips internal IDs and returns a Content-Disposition JSON attachment. POST /import (registered before /:id to avoid route shadowing) accepts multipart/form-data, creates parser and steps, returns 201. Client: export button on detail page, import file picker on parsers list. |
| 012 | [Step code templates picker](012-step-code-templates.md) | completed | Static STEP_TEMPLATES array (5 entries: pagination traverser, category traverser, REST API extractor, product detail extractor, infinite scroll traverser) in a client-only module. Dropdown in ParserEditorPage filtered by step type; hidden once code is saved. |
| 013 | [Cron scheduled runs](013-cron-scheduled-runs.md) | completed | scheduledRuns table (one row per parser, UNIQUE parserId). SchedulerService polls every 60 s, skips already-running parsers, updates nextRunAt via cron-parser. Schedule GET/PUT/DELETE routes. SchedulePanel React component with cron preview. |
| 014 | [Webhook notifications](014-webhook-notifications.md) | completed | webhookUrl column on parsers table. WebhookService.fire() posts a WebhookPayload on runner 'complete' and 'stopped' events (best-effort, errors logged). URL configured via ParserSettingsPanel. |
| 015 | [Proxy rotation pool](015-proxy-rotation-pool.md) | completed | ProxyPoolService (round-robin) instantiated per worker from stepSettings.proxyPool. buildContextOptions accepts proxyOverride and injects it into Playwright context options. Pool rotates on every context rotation (scheduled and failure-triggered). |
| 016 | [Step code version history](016-step-version-history.md) | completed | stepVersions table (append-only). updateStep snapshots the prior code before overwriting when code changes and existing code is non-empty; snapshot errors are non-fatal. Versions list and restore API routes. StepVersionsPanel toggled by a History button in the editor. |
| 017 | [Editor modals and collapsible nav sidebar](017-editor-modals-and-collapsible-nav.md) | completed | Parser and step settings moved from inline expanding panels into modal overlays with explicit Save/Cancel. Shared Modal.tsx base. Sidebar collapses to 48px icon-only rail via navCollapsed in useSettings. |
| 018 | [React Router and client source layout](018-react-router-and-src-layout.md) | completed | Hash router replaced with react-router-dom v7 (BrowserRouter + Routes/Route). All nav callback props removed; pages call useNavigate/useParams directly. Source split into pages/ (route components, with per-page subfolders for page-specific panels) and components/ (shared: Layout, Modal, JsonEditor, motion/). |
